.top-bar.fixed {
	position: fixed;
	width: 100%;
	top: 0;
	z-index: 1024;
	box-shadow: 0 1rpx 6rpx rgba(0, 0, 0, 0.1);
}
.cu-form-group .title {
  min-width: calc(4em + 30rpx);
}
.cu-form-group picker::after {
  display:none;
}